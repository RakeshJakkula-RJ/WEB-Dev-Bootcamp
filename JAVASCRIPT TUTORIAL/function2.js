// function logic should be written in a way that the code can be re-used many times.

// to access the app name we have to initialize a variable 
// inside the function's constructor as a parameter.

function openApp(name){

    console.log("please open the", name);
}

openApp("Instagram");

openApp("whatsApp");

function Call(name){

    console.log("please call", name);

}

   Call("RJ")