// concatination (+)

// if we use "+" operator in between numbers it is addition operator,
// but if we use "+" in between to strings (to combine two strings) then it is "Concatination"

//ex;

let a = "Hello";
let b = "World";
let x = a+b;

console.log(x);
console.log(a+ " " +b);

